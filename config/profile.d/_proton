# ProtonDrive — auto-detect by email folder name
[ -d "$HOME/Library/CloudStorage" ] || return 0
export PROTON_DRIVE
PROTON_DRIVE=$(find "$HOME/Library/CloudStorage" -maxdepth 1 -type d -name "ProtonDrive-*@*-folder" 2>/dev/null | head -1)
