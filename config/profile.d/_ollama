# Ollama
command -v ollama >/dev/null 2>&1 || return 0
export OLLAMA_KEEP_ALIVE="60m"  # keep models warm in RAM (default is 5m)

alias model-review="${SETTINGS_REPO:-$HOME/code/isaackehle/settings}/scripts/model-review.sh"
