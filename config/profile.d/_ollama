# Ollama
command -v ollama >/dev/null 2>&1 || return 0
export OLLAMA_KEEP_ALIVE="5m"   # keep models warm in RAM (0 = immediate unload)

alias model-scout="${SETTINGS_BASE:-$HOME/code/isaackehle/settings}/model-scout.sh"
alias suggest-models="${SETTINGS_BASE:-$HOME/code/isaackehle/settings}/2-ai/profiles/suggest-models.sh"
