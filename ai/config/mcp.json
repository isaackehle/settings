{
  "mcpServers": {
    "apple-reminders": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-apple-events"
      ]
    },
    "home-assistant": {
      "type": "http",
      "url": "${HA_MCP_URL}"
    }
  }
}