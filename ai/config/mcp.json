{
  "mcpServers": {
    "apple-reminders": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-server-apple-events"
      ]
    },
    "home-assistant": {
      "type": "http",
      "url": "${HOMEASSISTANT_MCP_URL}"
    }
  }
}