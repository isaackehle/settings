{
  "$schema": "https://app.kilo.ai/config.json",

  // "kilo-code.new.model.providerID": "LM Studio",
  // "kilo-code.new.model.modelID": "qwen_qwen3-30b-a3b",
  "permission": {
    "bash": "allow",
  },

  "model": "ollama/qwen3-coder-30b-a3b:q4",

  // Autocomplete can point at a different (usually smaller/faster) provider
  // profile independently — see kilo-code.new.autocomplete.provider / .model
  // if you want that to differ from the chat default above.
  "small_model": "ollama/qwen2.5-coder:1.5b",
}
